<div class="flex items-center justify-between flex-row-reverse">
    <form action="/favourites" method="POST"> <!-- This post req handles by only postController (keyNotes in learningNotes.txt in 5th point)-->
        <input type="hidden" name="id" value="<%= home._id %>">
        <% if (isLoggedIn) { %>
        <button class="bg-rose-400 text-white px-4 py-2 rounded-lg hover:bg-rose-500 transition-colors">
            Add To Favourite
        </button>
        <% } %>
    </form>
</div>